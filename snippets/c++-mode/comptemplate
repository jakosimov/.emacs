# -*- mode: snippet -*-
# name: template
# key: template
# --

#include <iostream>
#include <vector>
#include <algorithm>
#include <math.h>
#include <tuple>

#define rep(i, a, b) for (int i = a ; i < b ; i++)
#define iter(i, arr) for (int i = 0 ; i < arr.size() ; i++)
#define trav(a, arr) for (auto a : arr)
#define all(arr) arr.begin(), arr.end()
#define sz(arr) (int)(arr).size()

#define DEBUG

using std::vector;
using std::string;
using std::sort;
using std::cout;
using std::endl;
using std::max;
using std::min;
using std::get;

typedef long long ll;
typedef std::pair<ll,ll> Pair;
typedef vector<ll> vi;
typedef vector<Pair> vii;


int main() {

  return 0;
}



